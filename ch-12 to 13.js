

// JavaScript implementation of
	// the above approach

	
        let latterType = prompt ("type tour corector")
		let numType = ""
            numType = Number(numType)
        if (latterType >= "A" && latterType <= "Z"){
            alert (`${latterType} is upar casse`)
        }
       if(latterType >= "a" && latterType <= "z"){
            alert (`${latterType} is lower casse`)   
        }
         else(numType == 0>1000000000)
            alert (`${numType} this is a number`)   
        